#include<stdio.h>
void main()
{
	
	//varible declaratoin
	int a,b,s,d,m,D,p;
	a = 45;
	b = 44;
	//calculation
	 s = a+b;
	 d = a-b;
	 m = a*b;
	 D = a/b;
	 p = a%b;
	 //output
	 printf("the sum of %d and %d is %d\n",a,b,s);
	 printf("the difference bt %d and %d is %d\n",a,b,d);
	 printf("the multiplayed by %d and %d is %d\n",a,b,m);
	 printf("the devided by %d and %d is %d\n",a,b,D);
	 printf("the reminder by %d and %d is %d\n",a,b,p);
	  
}
